/**
 * Pi RPC runner package.
 *
 * P1 only stages this workspace. The `pi --mode rpc` JSONL client, session
 * lifecycle and stream event mapping arrive in P4.
 */
export const PI_RUNNER_PACKAGE_VERSION = '0.1.0';
